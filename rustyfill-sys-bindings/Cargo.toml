[package]
name = "rustyfill-sys-bindings"
version.workspace = true
edition = "2024"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
syn = { workspace = true }
quote = { workspace = true }
proc-macro2 = { workspace = true }

[dev-dependencies]
# rustdoc-types 0.33 is the release matching format_version 37 (Rust 1.85); it serves as an
# independent reference implementation for the v37 wire shapes exercised by the tests.
rustdoc-types = "=0.33.0"
tempfile = "3"
